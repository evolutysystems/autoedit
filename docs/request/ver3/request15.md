# Request
以下のデザイン修正を行ってください。
- メイン
　設定ボタンとクリップ用・アーカイブ用のウィンドウとくっついているので、設定ボタンの下に余白を作ってほしい。
　チェックボックスをチェックすると、背景色は変化するがチェックマークが付かないので、背景色がオレンジになった際にはチェックマークも付けてほしい。
　QComboBoxのデザインが、「▽」の部分が浮き出ている。docs/layout/index.htmlの部品カタログと同じようにしてほしい。
　・クリップ用
        以下の画面構成を、画面の上から構成してほしい。
            画面の上部４０％を占める大きさで以下の文言を記載する。
                「ドラッグ＆ドロップでファイルを選択」
                ファイルをドラッグ＆ドロップするアイコン等あればさらにいい。
                既存のファイル選択は隠す。参照ボタンも隠す。
                画面のどこにでもファイルをドラッグ＆ドロップすればファイルが選択される。
                ドラッグ＆ドロップした場合、「ドラッグ＆ドロップでファイルを選択」の文言がファイル名になる。
                ファイル名の右側に×ボタンが表示され、押下するとファイル選択がクリアされ、また文言が戻る。
            
            既存の「編集の続き」（保存されたプロジェクトはありません）、開く・一覧ボタンは削除する。
            代わりに以下のボタンを画面中央に追加する。
                「続きから」
                このボタンを押下すると、既存ボタンの「一覧...」と同じ動作をする。
                選択すると、「ドラッグ＆ドロップでファイルを選択」文言に、選択したプロジェクト名が表示される。
                プロジェクト名の右側に×ボタンが表示され、押下するとファイル選択がクリアされ、また文言が戻る。

            次に実行ボタンを配置する。デザインは既存と同じ。ただ画面中央に配置する。

            次にプログレスバーを配置する。デザインは既存と同じ。

            次に実行状況を表すラベルを配置する。デザインは既存と同じ。

（追加）
上部40%と記載したが、まだ画面下部に余白が残っているので、60%にしてください。

# Output
resolve15.mdとして修正設計書、resolve15.htmlとして画面イメージを作成してください。